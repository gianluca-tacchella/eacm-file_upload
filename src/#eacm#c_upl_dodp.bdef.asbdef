projection implementation in class /EACM/BP_C_UPL_DODP unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for /EACM/C_UPL_DODP alias /eacm/cUplDodp
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action uploadFileData;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}